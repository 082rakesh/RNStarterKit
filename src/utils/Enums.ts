export const enum APIStatus {
  LOADING = 'loading',
  SUCCESS = 'succeeded',
  REJECTED = 'failed',
}

export const enum TextTypes {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  HEADING = 'heading',
}
